# Jules Validation — T3.1: /gate/ landing page

**Branch:** `feat/hitl-gate-landing`
**Validate against the Cloudflare branch preview URL only — not quirgs.com production.**

---

## Checklist

### Route & build
- [ ] `/gate/` resolves — no 404
- [ ] `npm run build` passes — no Astro type or schema errors
- [ ] Page title is `Gate | Quirgs`
- [ ] Route appears in SiteMenu overlay (`[≡]`) under the resources group — not in the primary inline NavBlock row

### Content
- [ ] `admin@quirgs ~/gate$` terminal prompt renders in the page header
- [ ] "Skills teach governance. The gate enforces it." appears verbatim
- [ ] Page has four labelled sections: what it is, why it exists, how it works, early access
- [ ] How-it-works section lists the 3-step flow (POST → review → approve/reject)
- [ ] Early access CTA links to the /support/ page
- [ ] Links to `/hitl/` and `/review/` resolve — no 404
- [ ] Link to `/demo/` present and resolves (may be a stub if T3.2 not yet merged — confirm it does not 404 on this branch)

### Terminal UI
- [ ] BaseLayout is used — terminal chrome, JetBrains Mono, green accent `#4ade80`
- [ ] No fade/slide CSS transitions — terminal aesthetic
- [ ] No inline `<script>` block present (no CSP hash change needed); if one is added, confirm the hash is updated in `public/_headers`

### Repo hygiene
- [ ] No changes to `public/guides/` — SEO-indexed legacy files untouched
- [ ] No changes to `src/pages/index.astro` — boot sequence out of scope for this PR
- [ ] CHANGELOG.md entry present under `feat/hitl-gate-landing`
- [ ] This file (`.jules/T3.1-gate-landing-validation.md`) committed with the PR
