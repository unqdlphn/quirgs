# Jules Validation — T3.2: /demo/ queue page

**Branch:** `feat/hitl-demo-queue`
**Validate against the Cloudflare branch preview URL only — not quirgs.com production.**

---

## Checklist

### Route & build
- [ ] `/demo/` resolves — no 404
- [ ] `npm run build` passes — no Astro type or schema errors
- [ ] Page title is `Demo Queue | Quirgs`
- [ ] Route appears in SiteMenu overlay (`[≡]`) under the resources group — not in the primary inline NavBlock row

### Content
- [ ] `[DEMO]` marker clearly visible — makes explicit this is not a live queue
- [ ] Exactly 3 demo events render with the following types (order newest-first):
  - `ai-transparency-notice`
  - `pii-exposure-check`
  - `eu-ai-act-classification`
- [ ] Each event card shows: type, item, stage, frameworks, received timestamp
- [ ] No token input block present — /demo/ requires no auth
- [ ] Approve and Reject buttons render on each card but are not wired to any API
- [ ] Clicking Approve or Reject shows a demo-mode inline message (not a network error, not a JS exception)
- [ ] CTAs at bottom: "→ /hitl/" and "→ /review/" both resolve — no 404

### Network
- [ ] No fetch/XHR calls to the HITL Gate Worker (`quirgs-hitl-gate.elbrigante9.workers.dev`) on page load — confirm in Network tab or with `curl` response inspection
- [ ] Page fully functional with no outbound API calls

### Terminal UI
- [ ] BaseLayout is used — terminal chrome, JetBrains Mono, green accent `#4ade80`
- [ ] Event cards match /review/ visual style
- [ ] No inline `<script>` block without a corresponding CSP hash update in `public/_headers`; prefer an external script file (`public/demo.js`) to avoid CSP hash churn

### Repo hygiene
- [ ] No changes to `public/guides/` — SEO-indexed legacy files untouched
- [ ] No changes to `src/pages/index.astro` — out of scope for this PR
- [ ] No changes to `workers/hitl-gate/index.js` — demo is fully static, no Worker changes
- [ ] CHANGELOG.md entry present under `feat/hitl-demo-queue`
- [ ] This file (`.jules/T3.2-demo-queue-validation.md`) committed with the PR
